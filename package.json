{
  "name": "gesture-boids-simulator",
  "version": "1.0.0",
  "description": "Browser-based Boids flock simulation with mouse and optional camera gesture controls.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
